# VELVET-STATE — read first each session

DIRECTION: This hub CONTINUES as a Next.js + Supabase app on Vercel. Do NOT assume any move to
Lovable or bolt. Alongside finishing features, the active goal is to MAKE THE SITE LOAD FASTER.

See VELVET-MEMORY.md (in outputs) for the full record. Quick state:
- SEO is LIVE (Ahrefs + Google Search Console). All other services are DEMO, honestly tagged.
- Built recently: per-client customizable KPI (velvet-28), "estimate" labels on ASO competitive
  data, a new super-admin OVERVIEW (/overview) with health cards, Ask Velvet, what-needs-attention,
  clients table, departments, platform usage; a CLIENT drawer (8 tabs) and a SERVICE drawer;
  team-member view moved to /my-work; /dashboard is now a router.
- Nav: Overview + My Work replaced the old single Dashboard link.
- Next: finish Stage 1 (doc editor + roadmap, maturity scoring, manual data path, creative
  sourcing, rebook, review loop, feedback finishing, real Platform Usage), then security, then
  SPEED (cache external data, server-render, fetch-once, lazy-load), then connect real sources.
- Hard rules unchanged (no em-dashes; never fake data; deploy whole project; getUser(token);
  task privacy). Next SQL number: velvet-29.
